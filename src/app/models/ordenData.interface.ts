// Define una interfaz para el objeto de orden que devuelve el backend
export interface OrdenData {
  codEmp: number;
  codSed: number;
  codTCl: number;
  numOrd: number;
  codPer: number;
  nomCom: string;
}